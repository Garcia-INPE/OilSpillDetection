# SHA256 checksums for DS files
# Generate with (run from DS directory):
#   find . -type f ! -name "checksums.sha256" -print0 | sort -z | xargs -0 sha256sum > checksums.sha256
#
# After generation, each line should look like:
# <sha256_hash><space><space>./relative/path/to/file
